MODULE_NAME := sensor_sc301IoT_t31
OUT := $(MODULE_NAME)

DIR=sensors/$(SOC)

SRCS := $(DIR)/sc301IoT/sc301IoT.c

OBJS := $(SRCS:%.c=%.o) $(ASM_SRCS:%.S=%.o)

$(OUT)-objs := $(OBJS)

obj-m += $(OUT).o
