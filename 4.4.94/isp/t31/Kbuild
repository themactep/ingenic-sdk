ISP_FW_VER=1.1.5.2

ifeq ($(SOC_FAMILY),c100)
        ISP_FW_VER=2.1.0
endif

COMPILER_VER=540

SRCS := $(DIR)/tx-isp-debug.c \
	$(DIR)/tx-isp-module.c

OBJS := $(SRCS:%.c=%.o) $(ASM_SRCS:%.S=%.o)
OBJS += sdk/$(SOC_FAMILY)/lib$(SOC_FAMILY)-firmware-$(ISP_FW_VER)-$(COMPILER_VER).a

$(OUT)-objs := $(OBJS)

obj-m += $(OUT).o
